name: Build

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            platform: win
          - os: macos-latest
            platform: mac
          - os: ubuntu-latest
            platform: linux

    runs-on: ${{ matrix.os }}
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Create placeholder icon (Windows)
        if: matrix.platform == 'win' && !hashFiles('resources/icon.ico')
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path resources | Out-Null
          Add-Type -AssemblyName System.Drawing
          $bmp = New-Object System.Drawing.Bitmap(256, 256)
          $g = [System.Drawing.Graphics]::FromImage($bmp)
          $g.Clear([System.Drawing.Color]::FromArgb(99, 102, 241))
          $g.Dispose()
          $icon = [System.Drawing.Icon]::FromHandle($bmp.GetHicon())
          $fs = New-Object System.IO.FileStream("resources/icon.ico", [System.IO.FileMode]::Create)
          $icon.Save($fs)
          $fs.Close()
          $icon.Dispose()
          $bmp.Dispose()

      - name: Create placeholder icon (Linux)
        if: matrix.platform == 'linux' && !hashFiles('resources/icon.png')
        run: |
          mkdir -p resources
          convert -size 256x256 xc:'#6366F1' resources/icon.png

      - name: Install dependencies
        run: npm ci

      - name: Build (Windows)
        if: matrix.platform == 'win'
        run: npm run dist -- --win --publish never

      - name: Build (macOS)
        if: matrix.platform == 'mac'
        run: npm run dist -- --mac --publish never

      - name: Build (Linux)
        if: matrix.platform == 'linux'
        run: npm run dist -- --linux --publish never

      - name: Upload artifacts (Windows)
        if: matrix.platform == 'win'
        uses: actions/upload-artifact@v4
        with:
          name: dist-win
          path: |
            dist/PDFDecomposer-Setup-*.exe
            dist/PDFDecomposer-portable.exe
          if-no-files-found: error

      - name: Upload artifacts (macOS)
        if: matrix.platform == 'mac'
        uses: actions/upload-artifact@v4
        with:
          name: dist-mac
          path: dist/PDFDecomposer-*.dmg
          if-no-files-found: error

      - name: Upload artifacts (Linux)
        if: matrix.platform == 'linux'
        uses: actions/upload-artifact@v4
        with:
          name: dist-linux
          path: dist/PDFDecomposer-*.AppImage
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/**
          generate_release_notes: true
